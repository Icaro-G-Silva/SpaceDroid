package com.example.launchcontrol.spacex.domain.enums

class SpaceXEnum {
    enum class InitialYear(val year: Int) {
        INITIAL_YEAR(2006)
    }
    enum class YearPattern(val yearPattern: String) {
        YEAR_PATTERN("yyyy")
    }
}