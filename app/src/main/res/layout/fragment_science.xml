<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".isswiki.iss_sections.science.ScienceFragment"
    android:background="@color/lighter_background">

    <TextView
        android:id="@+id/iss_science_title"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginVertical="48dp"
        android:gravity="center_horizontal"
        android:textSize="24sp"
        android:textColor="@android:color/primary_text_dark"
        tools:text="ISS's Scientific Researches\n(NASA Only)"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <ExpandableListView
        android:id="@+id/iss_science_expandablelist"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginVertical="16dp"
        tools:listheader="List Header"
        android:indicatorStart="?android:attr/expandableListPreferredItemIndicatorLeft"
        android:divider="@color/darker_background"
        android:dividerHeight="0.5dp"
        app:layout_constraintTop_toBottomOf="@id/iss_science_title"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>